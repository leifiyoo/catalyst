# CatalystAnalytics Configuration
# ================================
# This plugin writes analytics data to a JSON file that the
# Catalyst Electron app reads directly. No API or network needed.

# Data settings
data:
  # How many days to keep detailed data
  retention-days: 30
  # How often to write analytics.json (in seconds)
  # The Catalyst app polls this file every 5-10 seconds
  save-interval-seconds: 30

# Collection intervals (in seconds)
collection:
  # TPS sampling interval
  tps-interval: 60
  # Memory sampling interval
  memory-interval: 60
  # Player count snapshot interval
  player-count-interval: 300

# Enable/disable individual tracking categories
# Toggle these to control what data gets collected
tracking:
  # Track player join/leave events
  track-player-joins: true
  # Track player Minecraft versions (protocol version detection)
  track-player-versions: true
  # Track player client brands (Fabric, Forge, Lunar, Vanilla, etc.)
  track-player-clients: true
  # Track player geolocation (country, region via IP lookup)
  track-geolocation: true
  # Track player operating system
  track-os: true
  # Track server TPS over time
  track-tps: true
  # Track server RAM usage over time
  track-ram: true
  # Track player playtime
  track-playtime: true
  # Track chat messages
  track-chat-messages: true
  # Track deaths and kills
  track-deaths-kills: true
  # Track blocks placed/broken
  track-blocks: true
  # Track commands executed
  track-commands: true

# Geolocation settings
geolocation:
  # Free API endpoint for IP geolocation
  api-url: "http://ip-api.com/json/{ip}?fields=status,country,regionName,city,isp"
  # Rate limit: max requests per minute (ip-api.com free tier allows 45/min)
  rate-limit: 40
  # Cache duration in minutes
  cache-minutes: 1440
